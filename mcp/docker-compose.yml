services:
  bexio-mcp:
    build: .
    container_name: bexio-mcp
    restart: unless-stopped
    ports:
      - "8080:8080"
    # No secrets: the bexio token is passed through per request from the client.
    # Optional single-user fallback:
    # environment:
    #   - BEXIO_API_TOKEN=${BEXIO_API_TOKEN}
